"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { useAuth } from "@/context/AuthContext";
export default function RegisterPage(){const router=useRouter();const {register}=useAuth();const [form,setForm]=useState({name:"",email:"",password:""});const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setError("");try{await register(form);router.push("/login")}catch(err:any){setError(err.message)}}return <main className="section"><div className="container" style={{maxWidth:480}}><form className="card" onSubmit={submit}><h1>Đăng ký</h1>{error&&<div className="error">{error}</div>}<input className="input dark-input" placeholder="Họ tên" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><br/><br/><input className="input dark-input" type="email" placeholder="Email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required/><br/><br/><input className="input dark-input" type="password" placeholder="Mật khẩu" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required/><br/><br/><button className="btn" style={{width:"100%"}}>Đăng ký</button></form></div></main>}
