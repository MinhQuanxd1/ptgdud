"use client";
import { useRouter, useSearchParams } from "next/navigation";
import { useState } from "react";
export default function ProductFilters(){const router=useRouter();const params=useSearchParams();const [keyword,setKeyword]=useState(params.get("keyword")||"");const [category,setCategory]=useState(params.get("category")||"all");const [maxPrice,setMaxPrice]=useState(params.get("maxPrice")||"");function apply(){const q=new URLSearchParams();if(keyword)q.set("keyword",keyword);if(category&&category!=="all")q.set("category",category);if(maxPrice)q.set("maxPrice",maxPrice);q.set("page","1");router.push(`/products?${q.toString()}`)}return <div className="card" style={{background:"#fff",color:"#111827",marginBottom:26}}><div className="row"><input className="input" placeholder="Tìm sản phẩm..." value={keyword} onChange={e=>setKeyword(e.target.value)}/><select value={category} onChange={e=>setCategory(e.target.value)}><option value="all">Tất cả</option><option value="shirt">Áo</option><option value="pants">Quần</option><option value="hoodie">Hoodie</option><option value="shoes">Giày</option></select><input className="input" type="number" placeholder="Giá tối đa" value={maxPrice} onChange={e=>setMaxPrice(e.target.value)}/><button className="btn" onClick={apply}>Lọc</button></div></div>}
